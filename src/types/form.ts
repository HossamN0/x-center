export interface LoginTypes {
    email: string;
    password: string;
}

export interface RegisterTypes {
    first_name: string;
    second_name: string;
    email: string;
    phone: string;
    password: string;
}