export enum Languages {
    EN = 'en',
    AR = 'ar',
}

export enum Directions {
    LTR = 'ltr',
    RTL = 'rtl',
}

export enum InputsType {
    TEXT = 'text',
    PASSWORD = 'password',
    EMAIL = 'email',
    PHONE = 'phone',
}

export enum Routes {
    HOME = '',
    CENTERS = 'centers',
    ABOUT = 'about',
    CONTACT = 'contact',
    AUTH = 'auth',
}

export enum Pages {
    LOGIN = 'login',
    SIGNUP = 'register',
}

export enum Forms {
    LOGIN = 'login',
    SIGNUP = 'register',
}