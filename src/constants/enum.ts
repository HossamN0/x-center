export enum Languages {
    EN = 'en',
    AR = 'ar',
}

export enum Directions {
    LTR = 'ltr',
    RTL = 'rtl',
}

export enum Routes {
    HOME = '',
    CENTERS = 'centers',
    ABOUT = 'about',
    CONTACT = 'contact',
    AUTH = 'auth',
}

export enum Pages {
    LOGIN = 'login',
    SIGNUP = 'signup',
}